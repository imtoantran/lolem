!!!
%html
  %head
    <meta charset="utf-8">
    %title #{@page_title} - #{Shoppe.settings.store_name}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='http://fonts.googleapis.com/css?family=Roboto:400,300,300italic,400italic,500,500italic,700,700italic&subset=vietnamese' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=PT+Serif&subset=vietnamese' rel='stylesheet' type='text/css'>
    = stylesheet_link_tag asset_url "fonts/fontello/css/fontello"
    = stylesheet_link_tag asset_url "plugins/rs-plugin/css/settings"
    = stylesheet_link_tag :application
    = csrf_meta_tags
    = yield :head
  %body
    .scrollToTop.circle
      %i.icon-up-open-big
/ page wrapper start
/ ================
.page-wrapper
  = render 'shared/header'
  -#= render 'shared/breadcrumb'
  = yield :banner
  = yield :under_banner
  = yield :services
  #page-start
  / section start
  = yield
  = yield :bottom_a
  = yield :bottom_b
  = yield :bottom_c
  = yield :bottom_d
  / footer top end
  / section end
  / footer start (Add "dark" class to #footer in order to enable dark footer)
  / ================
  = render 'shared/footer'
  = display_flash
  = javascript_include_tag :application
