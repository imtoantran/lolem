- @page_title = t('shoppe.services.services')
= content_for :header do
  %p.buttons= link_to t('shoppe.services.back_to_services') , :services, :class => 'button'
  %h2.services= t('shoppe.services.services')
= render 'form'
